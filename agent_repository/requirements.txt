# --- Core Application & Image Generation ---
gradio
torch
diffusers[torch]
transformers
accelerate
safetensors

# --- Utilities ---
tqdm
requests

# --- The Brain (Manual Install Recommended) ---
# For full functionality and GPU acceleration, please install llama-cpp-python manually
# after installing the packages above. Use the following command tailored for your system:
# CMAKE_ARGS="-DGGML_CUDA=on" FORCE_CMAKE=1 pip install llama-cpp-python --no-cache-dir
#
# # llama-cpp-python